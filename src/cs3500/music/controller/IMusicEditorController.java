package cs3500.music.controller;

/**
 * Interface for the MusicEditor controller.
 */
public interface IMusicEditorController {
  /**
   * Start the program, basically. Give control to the controller, start up view based on model.
   */
  void go();
}
